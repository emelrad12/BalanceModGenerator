Simple tool to generate mods based on some rules, so you dont have to manually fix all your mods each game version, but simply regen them.

Currently it is very barebones, and using hardcoded paths, wip
